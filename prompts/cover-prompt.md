# Cover Prompt Task

Use `outputs/<slug>/public/post.md` and `outputs/<slug>/work/brief.md` to create a cover image prompt.

Produce `outputs/<slug>/public/cover-prompt.md` with:

- Topic and visual metaphor
- Composition
- Color and mood
- Style constraints
- Elements to avoid

Do not generate the actual image.
